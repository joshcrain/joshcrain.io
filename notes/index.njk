---
title: Notes
metaTitle: Josh Crain’s Notes
description: Writings. Some personal and some related to work as a web and product designer. 
---
<p class="lead">Writing to share with you. </p>
<ul class="list--articles">
    {% set items = collections.notes | reverse %}
    {% for post in items %} 
    <li>
        <a href="{{ post.url }}" class="text--larger">{{ post.data.title }}</a>
        <p><span class="small-caps"><time datetime="{{ post.date | date('YYYY-MM-DD HH:mm') }}">{{ post.date | date("DD MMM YYYY") }}</time> ⁃ <span class="article--tags">{% for tag in post.data.tags %}{% if tag != 'notes' %}<span> {{tag}}</span>{% endif %}{% endfor %}</span></span> <br>
            {{ post.data.description }}</p>
    </li>
    {% endfor %}
</ul>
